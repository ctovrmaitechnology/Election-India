import React from 'react';
import { MapPropType } from '../types/types';
declare const KarnatakaMap: ({ className, size, mapColor, strokeColor, strokeWidth, hoverColor, onClick, }: MapPropType) => React.JSX.Element;
export default KarnatakaMap;
